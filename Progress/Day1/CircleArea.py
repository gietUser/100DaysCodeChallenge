# area of circle

import math

radius = float(input("Enter the radius:"))
area = math.pi * radius * radius
permi = math.pi * 2 * radius

print("Area of the Circle: {0:.2f}".format(area))
print("Perimeter of the Circle: {0:.2f}".format(permi))

